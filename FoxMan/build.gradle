apply plugin: 'com.android.library'

def config = rootProject.ext.android //配置
def librarys = rootProject.ext.dependencies //依赖
android {
    compileSdkVersion 26
    buildToolsVersion config.buildToolsVersion

    defaultConfig {
        minSdkVersion config.minSdkVersion
        targetSdkVersion config.targetSdkVersion
        versionCode config.versionCode
        versionName config.versionName
        javaCompileOptions {
            annotationProcessorOptions {
                arguments = [ moduleName : project.getName() ]
            }
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
        encoding = 'UTF-8'
    }


    buildTypes {
        debug {
        }
    }


    configurations.all {
        resolutionStrategy.eachDependency { DependencyResolveDetails details ->
            def requested = details.requested
            if (requested.group == 'com.android.support') {
                if (!requested.name.startsWith("multidex")) {
                    details.useVersion '26.0.2'
                }
            }
        }
    }


}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile librarys.rxjava
    compile librarys.rxbind2
    compile librarys.rxbind2_v4
    compile librarys.rxbind2_v7
    compile librarys.rxandroid
    compile librarys.rxlifecycle
    compile librarys.rxlifecycle_components
    compile librarys.rxlifecycle_com_perfer
    compile librarys.rxpermissions
    compile librarys.retrofit
    compile librarys.adapter_rxjava
    compile librarys.converter_gson
    compile librarys.converter_str
    compile librarys.logging_interceptor
    compile librarys.blockcanary
    compile librarys.swipebacklayout
    compile librarys.gson
    compile librarys.crashreport
    compile librarys.nativecrashreport
    compile librarys.smart_refresh_layout
    compile librarys.smartrefreshheader
    //compile librarys.orhanobut_logger
    compile librarys.countdownview
    compile librarys.flowlayout
    compile librarys.androideventbus
    compile librarys.ninegridview
    compile librarys.BaseRecyclerViewAdapterHelper
    compile librarys.cardviewV7
    compile librarys.multidex
    compile librarys.glide
    annotationProcessor librarys.glide_processor
    compile librarys.fragmentation
    compile librarys.glide_transformations
    compile librarys.fastjson
    compile librarys.FlycoTabLayout_Lib
    compile librarys.FlycoRoundView_Lib
    compile librarys.appcompatV7
    compile librarys.banner
    compile librarys.recovery
    compile librarys.versitonCheck
    // 版本检测
    compile librarys.xtablayout
    compile librarys.time_line_view
    compile librarys.basePopup
    compile librarys.arouterApi
    compile librarys.tinypinyin
    compile librarys.AndroidPickerView
    compile librarys.utilcode
    compile librarys.zxingCore
    compile librarys.richtext
    compile librarys.aviLibrary
    compile librarys.circleImageView
    compile librarys.pullLoadMoreRecyclerView
    compile librarys.switchButton
    compile librarys.recyclerViewSwipe
    compile librarys.constraintLayout
    compile librarys.gifDrawable
    compile librarys.butterKnife
    compile librarys.floatingActionButton
    compile librarys.flycoTabLayout
    compile librarys.meterialRipple
    compile librarys.simpleAndroidEventBus
    compile librarys.bubbleSeekBar
    compile librarys.kotlinStdlibJre7
    compile librarys.bgaBanner
    compile librarys.MPAndroidChart
    compile librarys.netmonitor
    compile librarys.magicIndicator
    compile librarys.luban
    compile librarys.roundedImageView
    compile librarys.badgeview
    compile project(':FoxArouter')

}





